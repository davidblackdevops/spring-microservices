spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://config-server:8888
      name: api-gateway
      profile: default
      label: main
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 2000
        multiplier: 1.5
        max-interval: 10000