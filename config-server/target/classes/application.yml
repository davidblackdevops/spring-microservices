server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/example/bookstore-config-repo
          default-label: main
          search-paths:
            - config
management:
  endpoints:
    web:
      exposure:
        include: health,info,env