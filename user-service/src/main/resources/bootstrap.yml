spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://config-server:8888
      name: user-service
      profile: default
      label: main
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 2000
        multiplier: 1.5
        max-interval: 10000