server:
  port: 9000
spring:
  application:
    name: monitoring
  config:
    import: optional:configserver:http://config-server:8888/
management:
  endpoints:
    web:
      exposure:
        include: health,info