server:
  port: 8081
spring:
  application:
    name: book-service
  config:
    import: optional:configserver:http://config-server:8888/
  datasource:
    url: jdbc:postgresql://localhost:5432/bookdb
    username: postgres
    password: password
management:
  endpoints:
    web:
      exposure:
        include: health,info