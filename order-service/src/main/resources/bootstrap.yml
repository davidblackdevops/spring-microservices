spring:
  application:
    name: order-service
  cloud:
    config:
      uri: http://config-server:8888
      name: order-service
      profile: default
      label: main
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 2000
        multiplier: 1.5
        max-interval: 10000