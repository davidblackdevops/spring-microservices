spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/example/bookstore-config-repo
          default-label: main
          search-paths:
            - config
server:
  port: 8888
management:
  endpoints:
    web:
      exposure:
        include: health,info,env